# GitHub Actions workflows

Los workflows (.github/workflows/ci.yml, deploy-api.yml) están en .github-workflows-backup/workflows/

No se subieron en el commit inicial porque el PAT usado solo tenía scope `repo`. Para activarlos, crea un PAT con scope `workflow` o súbelos vía la UI web de GitHub en cada archivo.
