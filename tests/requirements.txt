# TripCazador — Test suite dependencies
# Instalar: pip install -r tests/requirements.txt

pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
responses>=0.25          # Mock HTTP calls (sync - requests)
aioresponses>=0.7        # Mock HTTP calls (async - aiohttp)
httpx>=0.27              # FastAPI TestClient backend
requests>=2.31
fastapi>=0.111
pydantic>=2.7
aiohttp>=3.9
